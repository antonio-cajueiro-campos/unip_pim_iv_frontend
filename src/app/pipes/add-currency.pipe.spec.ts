import { AddCurrencyPipe } from './add-currency.pipe';

describe('AddCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new AddCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
