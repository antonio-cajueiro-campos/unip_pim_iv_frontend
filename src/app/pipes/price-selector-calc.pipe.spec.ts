import { PriceSelectorCalcPipe } from './price-selector-calc.pipe';

describe('PriceSelectorCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceSelectorCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
