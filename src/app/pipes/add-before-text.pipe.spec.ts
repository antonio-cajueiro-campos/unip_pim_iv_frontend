import { AddBeforeTextPipe } from './add-before-text.pipe';

describe('AddBeforeTextPipe', () => {
  it('create an instance', () => {
    const pipe = new AddBeforeTextPipe();
    expect(pipe).toBeTruthy();
  });
});
